---
nsd_user: "{{ __nsd_user }}"
nsd_group: "{{ __nsd_group }}"
nsd_service: "{{ __nsd_service }}"
nsd_package: "{{ __nsd_package }}"
nsd_conf_dir: "{{ __nsd_conf_dir }}"
nsd_zones_dir: "{{ __nsd_zones_dir }}"
nsd_db_dir: "{{ __nsd_db_dir }}"
nsd_conf_file: "{{ nsd_conf_dir }}/nsd.conf"
nsd_bin: "{{ __nsd_bin }}"
nsd_sock: "{{ __nsd_sock }}"
nsd_flags: ""
nsd_zones_inputdir: ""
nsd_role: "standalone"
nsd_listens: []
